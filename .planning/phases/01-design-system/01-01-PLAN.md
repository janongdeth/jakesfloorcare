---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06

must_haves:
  truths:
    - "style.css uses Sora/Outfit fonts and blue (#2a4592) / dark (#141414) palette — no Barlow Condensed, no copper, no cream"
    - "All shared component classes are present: header/nav, dropdowns, buttons (btn-blue, btn-ghost, btn-white), section-tag, section-title, section-desc, svc-card, testimonial, areas-list, cta-band, footer, mobile-sticky, reveal"
    - "Mobile tap highlights are suppressed on interactive cards (-webkit-tap-highlight-color: transparent)"
    - "Image long-press is disabled on content images (-webkit-touch-callout: none; user-select: none)"
    - "Mobile sticky CTA bar exists with Call Now + Free Quote buttons"
    - "Old copper/Barlow Condensed design system is fully replaced"
  artifacts:
    - path: "style.css"
      provides: "Complete shared design system — tokens, reset, all components"
      contains: "--blue: #2a4592"
  key_links:
    - from: "style.css :root"
      to: "All pages"
      via: "<link rel='stylesheet' href='/style.css'>"
      pattern: "--blue: #2a4592"
    - from: ".mobile-sticky"
      to: "mobile viewport"
      via: "@media (max-width: 768px) { .mobile-sticky { display: block } }"
      pattern: "mobile-sticky"
---

<objective>
Replace the old Barlow Condensed / copper / cream design system in style.css with the complete design system extracted from the homepage's inline styles.

Purpose: Every inner page currently loads style.css, which uses the wrong fonts, wrong colors, and wrong components. After this plan, style.css IS the homepage design system — ready for every page to use.

Output: A fully rewritten style.css containing all design tokens, resets, nav, buttons, section patterns, service cards, testimonials, areas list, CTA band, footer, mobile sticky bar, reveal animation, and responsive rules. The old Barlow/copper system is gone.
</objective>

<execution_context>
@C:/Users/janon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/janon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/janon/websites/jakesfloorcare/.planning/PROJECT.md
@C:/Users/janon/websites/jakesfloorcare/.planning/ROADMAP.md
@C:/Users/janon/websites/jakesfloorcare/.planning/STATE.md

The homepage inline styles (index.html lines 13–732) are the source of truth.
The old style.css uses Barlow Condensed, Source Serif 4, and copper/cream palette — it must be fully replaced.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite style.css with the full homepage design system</name>
  <files>style.css</files>

  <read_first>
    - C:/Users/janon/websites/jakesfloorcare/index.html (lines 13–732 — the complete inline &lt;style&gt; block is the source)
    - C:/Users/janon/websites/jakesfloorcare/style.css (current file — read fully before overwriting)
  </read_first>

  <action>
Completely replace style.css. The new file must contain every rule from the homepage's inline style block (index.html lines 13–732), adapted for shared use. Structure in this exact order:

1. **File header comment:**
   ```css
   /* ========================================
      JAKE'S FLOOR CARE — SHARED DESIGN SYSTEM
      Source: homepage inline styles (index.html)
      ======================================== */
   ```

2. **Reset + :root tokens** — Copy verbatim from homepage inline styles:
   - `*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }`
   - All `:root` CSS variables exactly as in homepage: `--blue: #2a4592`, `--blue-dark: #1d3268`, `--blue-light: #3a5bb0`, `--black: #0a0a0a`, `--dark: #141414`, `--gray-800: #2e2e2e`, `--gray-600: #555`, `--gray-400: #8a8a8a`, `--gray-300: #b0b0b0`, `--gray-200: #d5d5d5`, `--gray-100: #eee`, `--white: #fff`, `--off-white: #f8f8f8`, `--ff-heading: 'Sora', sans-serif`, `--ff-body: 'Outfit', sans-serif`, `--header-h: 70px`
   - `html`, `body`, `img`, `a`, `button`, `.container`, `.sr-only` base rules

3. **Header/Nav** — All rules from `/* HEADER */` section: `.site-header`, `.header-inner`, `.logo`, `.main-nav`, `.nav-arrow`, `.dropdown`, `.dropdown.wide`, `.dropdown-label`, `.has-dropdown:hover` states, `.header-phone`, `.header-cta`, `.menu-toggle`

4. **Buttons** — `.btn-blue`, `.btn-ghost`, `.btn-white` exactly as in homepage

5. **Sections** — `section { padding: 100px 0; }`, `.section-tag`, `.section-title`, `.section-desc`

6. **Service cards** — `.services`, `.services-header`, `.services-grid`, `.services-grid.coatings-grid`, `.services-grid.cleaning-grid` with all grid-column span rules, `.svc-card`, `.svc-card-bg`, `.svc-card::after`, `.svc-card:hover` states, `.svc-card-content`, `.svc-card-name`, `.svc-card-brief`, `.svc-card-arrow`, `.services-view-all`

   Add mobile tap highlight suppression directly to `.svc-card`:
   ```css
   -webkit-tap-highlight-color: transparent;
   ```

7. **Trust section** — `.trust`, `.trust-overlap`, `.trust-team`, `.trust-team-img`, `.trust-team-text h2`, `.trust-team-text p`

8. **Testimonials** — `.testimonials`, `.testimonial`, `.testimonial::before`, `.testimonial-text`, `.testimonial-author`

9. **Areas** — `.areas`, `.areas-layout`, `.areas-list`, `.areas-list li`, `.areas-list a`, `.areas-arrow`

10. **CTA band** — `.cta-band`, `.cta-title`, `.cta-sub`, `.btn-white` (already covered in buttons, don't duplicate), `.cta-phone`

11. **Footer** — `.site-footer`, `.footer-grid`, `.footer-brand`, `.footer-col h4`, `.footer-col ul`, `.footer-col li`, `.footer-col a`, `.footer-contact-item`, `.footer-bottom`

12. **Mobile sticky** — `.mobile-sticky`, `.mobile-sticky-inner`, `.mobile-sticky a`, `.sticky-call`, `.sticky-quote`

    The sticky bar must be `display: none` by default and shown only at mobile breakpoint via media query:
    ```css
    .mobile-sticky { display: none; ... }
    /* shown inside @media (max-width: 768px) */
    .mobile-sticky { display: block; }
    body { padding-bottom: 52px; }
    ```

13. **Reveal animation** — `.reveal`, `.reveal.visible` with transition values from homepage

14. **Page header** — Add `.page-header` block for inner pages (needed in Phase 2). Use the homepage's dark/blue palette, not the old copper design:
    ```css
    .page-header {
      margin-top: var(--header-h);
      background: var(--dark);
      padding: 60px 0;
    }
    .page-header .container { position: relative; z-index: 1; }
    .breadcrumbs {
      font-family: var(--ff-body); font-size: 12px; font-weight: 500;
      text-transform: uppercase; letter-spacing: 2px; color: var(--gray-400);
      margin-bottom: 16px;
    }
    .breadcrumbs a { color: var(--gray-300); text-decoration: none; transition: color 0.2s; }
    .breadcrumbs a:hover { color: var(--white); }
    .breadcrumbs .sep { margin: 0 8px; color: var(--gray-600); }
    .page-title {
      font-family: var(--ff-heading); font-weight: 700;
      font-size: clamp(36px, 4.5vw, 58px); color: var(--white);
      line-height: 1.05; margin-bottom: 12px;
    }
    .page-subtitle {
      font-family: var(--ff-body); font-size: 16px; font-weight: 300;
      color: var(--gray-400); max-width: 560px;
    }
    ```

15. **Content images — disable long-press** — Add this rule after the `img` base rule:
    ```css
    img.content-img {
      -webkit-touch-callout: none;
      user-select: none;
    }
    ```
    This applies to images that will get the `.content-img` class during Phase 2 page rebuilds.

16. **Responsive breakpoints** — Copy all `@media` rules from homepage exactly:
    - `@media (max-width: 1024px)` — tablet adjustments
    - `@media (max-width: 768px)` — mobile: show menu-toggle, show mobile-sticky, hide header-phone/header-cta, mobile nav slide-in, section padding reduction, footer column stack
    - `@media (max-width: 480px)` — small mobile tweaks

Do NOT include any of these from the old style.css: Barlow Condensed, Source Serif 4, `--copper`, `--charcoal`, `--warm-gray`, `--concrete`, `--sand`, `--cream`, `--off-white: #faf8f5` (off-white must be `#f8f8f8`), `.body::before` noise texture, `.page-header::before` diagonal lines pattern, `.btn-primary`, `.btn-outline`, `.section-label`, `.stagger` — none of these exist in the homepage design.
  </action>

  <verify>
    <automated>grep -c "\-\-blue: #2a4592" C:/Users/janon/websites/jakesfloorcare/style.css && grep -c "Barlow Condensed" C:/Users/janon/websites/jakesfloorcare/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep "--blue: #2a4592" style.css` returns 1 match
    - `grep "Barlow Condensed" style.css` returns 0 matches
    - `grep "copper" style.css` returns 0 matches
    - `grep "Sora" style.css` returns at least 1 match (in :root --ff-heading)
    - `grep "mobile-sticky" style.css` returns at least 3 matches (definition + inner + a selector)
    - `grep "webkit-tap-highlight-color" style.css` returns 1 match on .svc-card
    - `grep "webkit-touch-callout" style.css` returns 1 match on img.content-img
    - `grep "page-header" style.css` returns at least 1 match
    - File is larger than 400 lines (replacing ~200 line old file with full homepage system)
  </acceptance_criteria>

  <done>style.css is the complete homepage design system. Old copper/Barlow palette fully removed. Mobile UX rules embedded. All Phase 2 pages can link to this file and get the correct design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Google Fonts loaded from external CDN; no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Google Fonts CDN | accept | Static site; fonts are decorative only, no auth or data involved |
| T-01-02 | Information Disclosure | CSS file contents | accept | No secrets in CSS; design tokens are public by nature |
</threat_model>

<verification>
After task completes:
1. `grep "--blue: #2a4592" style.css` — returns 1 line
2. `grep "Barlow Condensed" style.css` — returns 0 lines (old design fully gone)
3. `grep -c "\.mobile-sticky" style.css` — returns 3+ lines
4. `grep "webkit-tap-highlight-color" style.css` — returns 1 line
5. `grep "webkit-touch-callout" style.css` — returns 1 line
6. Open index.html in a browser while temporarily adding `<link rel="stylesheet" href="/style.css">` in addition to the inline styles — page must not visually break (sanity check that class names are compatible)
</verification>

<success_criteria>
style.css contains the full Sora/Outfit + blue/dark design system extracted from the homepage. Old Barlow Condensed/copper palette is entirely absent. Mobile sticky CTA, tap-highlight suppression, and image long-press rules are present. Any inner page can link to this file and get the correct design tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md` using the summary template.
</output>
